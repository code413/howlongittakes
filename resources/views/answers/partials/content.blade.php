<div class="answer">
    According to <a href="#" class="text-body dash-underline">2 references</a> it takes
    on average
    <h2 class="d-inline text-primary">{{ $question->answers->first()->value }} {{ $question->answers->first()->unit->name }}s</h2>
    to {{ $question->content }}.
</div>